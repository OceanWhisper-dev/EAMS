# EAMS2026 项目完整文档

## 文档版本：V1.1
## 最后更新：2026-05-26
## 当前版本号：v0.1.260526

---

## 第一部分：项目概述

### 1.1 项目名称
**EAMS2026**（Enterprise Asset & Approval Management System 2026）

### 1.2 项目目标
将现有的多个C#程序（BS+CS）功能整合重构为一个**局域网部署的BS架构**企业管理系统。采用**先跑通、再优化**的策略，后期可根据需要将高并发模块（如考勤同步）用Rust改写，但需提前做好接口抽象。

### 1.3 部署环境
- **操作系统**：Windows（开发环境） / Debian 12（生产环境）
- **Web服务器**：Nginx（反向代理 + 静态资源）
- **应用服务器**：.NET 8（Kestrel）
- **数据库**：PostgreSQL（主库）
- **外部数据源**：
  - SQL Server 2005（用友U8 V8.71 ERP）——**读写**
  - SQL Server 2012+（考勤机）——**只读**
  - 钉钉考勤API（新增）

### 1.4 技术栈（v0.1.260526 已实现）
| 层级 | 技术 |
|------|------|
| 前端 | Vue 3 + Vite + TypeScript + Pinia + Vue Router + Element Plus |
| 后端 | .NET 8 Web API + Dapper（不使用EF Core） |
| 数据库 | PostgreSQL（主库），SQL Server（外部） |
| 认证 | JWT（Bearer Token） + BCrypt 密码哈希 |
| 打印 | PuppeteerSharp（HTML → PDF） |
| 定时任务 | BackgroundService / Quart.NET |
| 依赖注入 | .NET 8 内置DI |
| 权限控制 | 自定义权限策略 + 前端 v-permission 指令 |
| 响应压缩 | Gzip |

---

## 第二部分：功能矩阵（按优先级排序）

### 🔷 优先级1：基础域 —— 系统管理模块 ✅ 已完成（v0.1.260526）
> 先搭架子，再填业务。此模块已完成，已生成设计文档和使用手册。

| 编号 | 功能 | 需求描述 | 实现状态 |
|------|------|----------|----------|
| 1.1 | 部门管理 | 树形结构，支持多级部门。支持Excel导入导出。 | ✅ 已实现 |
| 1.2 | 员工档案 | 员工基本信息，与用户账号关联。支持Excel导入。 | ✅ 已实现 |
| 1.3 | 用户管理 | 账号绑定员工，支持启用/禁用。密码BCrypt加密。 | ✅ 已实现 |
| 1.4 | 角色管理 | RBAC标准模型，角色可分配菜单和按钮权限。 | ✅ 已实现 |
| 1.5 | 认证服务 | JWT登录认证，支持Token过期刷新。 | ✅ 已实现 |
| 1.6 | 操作日志 | 记录主要操作（增删改），含操作前后的数据变化。 | ✅ 已实现 |
| 1.7 | 导入导出 | 基于Excel模板的数据导入导出（部门和员工）。 | ✅ 已实现 |
| 1.8 | 打印服务 | 生成PDF并调用浏览器打印，支持自定义模板。 | ✅ 已实现 |
| 1.9 | 消息中心 | 站内信通知（审批提醒、系统通知）。 | ✅ 已实现 |
| 1.10 | 字典管理 | 统一维护下拉选项的键值对。 | ✅ 已实现 |
| 1.11 | 仪表盘 | 统计卡片展示，最近操作日志列表。 | ✅ 已实现 |
| 1.12 | 仪表盘配置 | 动态组件管理，支持自定义数据源（SQL/API/Count等）。 | ✅ 已实现 |

### 🔷 优先级2：考勤域 —— 考勤管理模块（待开发）
> 从考勤机（SQL Server 2012）和**钉钉**两个渠道获取考勤数据。现有考勤程序已正常运行，需整合优化。

| 编号 | 功能 | 需求描述 | 实现状态 |
|------|------|----------|----------|
| 2.1 | 考勤数据同步（考勤机） | 定时从SQL Server 2012拉取原始打卡记录到PG。 | ⏳ 待开发 |
| 2.2 | 考勤数据同步（钉钉） | 通过钉钉API获取打卡数据，支持增量同步。 | ⏳ 待开发 |
| 2.3 | 打卡记录查询 | 按员工、日期范围、来源（考勤机/钉钉）筛选。 | ⏳ 待开发 |
| 2.4 | 考勤异常处理 | 自动识别迟到/早退/缺卡，支持手动标记和备注。 | ⏳ 待开发 |
| 2.5 | 考勤统计报表 | 月度出勤统计、迟到/早退/旷工汇总，导出Excel。 | ⏳ 待开发 |
| 2.6 | 考勤规则配置 | 配置上下班时间、迟到/早退阈值、节假日、计薪日。 | ⏳ 待开发 |
| 2.7 | 多源数据合并 | 同一员工的钉钉和考勤机打卡数据按时间合并展示，无重复。 | ⏳ 待开发 |

### 🔷 优先级3：签呈审批模块（待开发）
> 替换现有的Word审批方式。第一期仅涉及**销售相关的价格、服务、返利**等业务。

| 编号 | 功能 | 需求描述 | 实现状态 |
|------|------|----------|----------|
| 3.1 | 签呈发起 | 选择业务类型，填写自定义事由表单，关联ERP单据。 | ⏳ 待开发 |
| 3.2 | 审批流程配置 | 管理员可配置多级审批节点、审批人（用户/角色）、会签/或签。 | ⏳ 待开发 |
| 3.3 | 待办工作台 | 查看待审批签呈，支持通过/驳回/转签。 | ⏳ 待开发 |
| 3.4 | 已办工作台 | 查看已处理的审批记录，可查看流程轨迹。 | ⏳ 待开发 |
| 3.5 | 审批记录 | 完整展示流程节点、操作人、时间、意见。 | ⏳ 待开发 |
| 3.6 | 自定义事由模板 | 管理员配置JSON Schema，前端动态渲染表单。 | ⏳ 待开发 |
| 3.7 | 签呈打印 | 严格按原Word样式生成PDF打印文件。 | ⏳ 待开发 |
| 3.8 | 审批通知 | 站内信通知审批人。 | ⏳ 待开发 |

### 🔷 优先级4：ERP协同域 —— 批量修改 + 单据签回（待开发）
> 目标U8 ERP（SQL Server 2005），必须兼容2005语法。

| 编号 | 功能 | 需求描述 | 实现状态 |
|------|------|----------|----------|
| 4.1 | 销售订单批量修改 | 批量修改**预发货日期**、**客户**（更换为另一客户）。 | ⏳ 待开发 |
| 4.2 | 销售发货单批量修改 | 批量修改**发货日期**、**客户**（更换为另一客户）。 | ⏳ 待开发 |
| 4.3 | 修改模板管理 | 保存常用修改规则（如：某客户订单全部改到另一客户）。 | ⏳ 待开发 |
| 4.4 | 修改日志 | 记录每次批量修改的详情（修改前值→修改后值）。 | ⏳ 待开发 |
| 4.5 | 签回单据查询 | 筛选待签回的ERP单据。 | ⏳ 待开发 |
| 4.6 | 签回执行 | 确认签回后，回写U8对应单据的签回状态。 | ⏳ 待开发 |
| 4.7 | 签回异常处理 | 记录失败原因，支持重试。 | ⏳ 待开发 |
| 4.8 | 签回日志 | 完整记录签回操作。 | ⏳ 待开发 |

### 🔷 优先级5：财务税务域 —— 金税管理模块（待开发）
> 区域：山东省济南市。优先尝试电子税务局API；若未开放，则使用现有程序的导入导出功能。

| 编号 | 功能 | 需求描述 | 实现状态 |
|------|------|----------|----------|
| 5.1 | 已开发票信息读取 | 从电子税务局读取已开发票，将发票号回写ERP的"金税号"字段。 | ⏳ 待开发 |
| 5.2 | 未开票发货单导入 | 将未开票的发货单数据导入电子税务局平台，确认开票。 | ⏳ 待开发 |
| 5.3 | 发票池 | 查询已同步的发票信息，支持导出。 | ⏳ 待开发 |

### 🔷 优先级6：决策支持域 —— 报表中心（待开发）
> 根据现有程序代码分析后具体设计。

---

## 第三部分：技术架构与关键设计（v0.1.260526 已实现）

### 3.1 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                      浏览器 (Chrome/Edge)                     │
└─────────────────────────────┬───────────────────────────────┘
                              │ HTTP
┌─────────────────────────────▼───────────────────────────────┐
│                      Nginx (Debian 12)                       │
│               静态资源 + 反向代理 + 负载均衡                   │
└─────────────────────────────┬───────────────────────────────┘
┌─────────────────────────────▼───────────────────────────────┐
│              EAMS2026.Api (.NET 8 Web API)                   │
│   Controllers / Middlewares / JWT / 全局异常 / 权限策略      │
└─────────────────────────────┬───────────────────────────────┘
┌─────────────────────────────▼───────────────────────────────┐
│           EAMS2026.Application (应用层)                       │
│   业务用例 / Commands & Queries / 验证 / 映射                  │
└─────────────────────────────┬───────────────────────────────┘
┌─────────────────────────────▼───────────────────────────────┐
│              EAMS2026.Domain (领域层)                         │
│   实体 / 值对象 / 仓储接口 / 业务规则                          │
└─────────────────────────────┬───────────────────────────────┘
┌─────────────────────────────▼───────────────────────────────┐
│          EAMS2026.Infrastructure (基础设施层)                 │
│   仓储实现(Dapper) / 连接工厂 / 外部服务(JWT/打印/钉钉/金税)   │
└───────────────┬─────────────┬─────────────┬─────────────────┘
                │             │             │
                ▼             ▼             ▼
        ┌───────────┐  ┌───────────┐  ┌───────────┐
        │ PostgreSQL│  │U8(SQL05)  │  │考勤(SQL12)│
        │   主库    │  │  读写     │  │  只读     │
        └───────────┘  └───────────┘  └───────────┘
                            └──────────┐
                                       ▼
                                  ┌─────────┐
                                  │ 钉钉API │
                                  └─────────┘
```

### 3.2 项目目录结构（v0.1.260526 实际结构）

```
EAMS2026/
├── src/
│   ├── EAMS2026.Domain/
│   │   ├── Entities/              # 17个领域实体
│   │   ├── Enums/                 # 枚举定义
│   │   ├── Interfaces/
│   │   │   └── Repositories/      # 仓储接口定义
│   │   └── Common/                # 通用模型（PagedResult等）
│   ├── EAMS2026.Application/
│   │   ├── Services/              # 10个业务服务
│   │   │   ├── AuthService.cs
│   │   │   ├── UserService.cs
│   │   │   ├── RoleService.cs
│   │   │   ├── DepartmentService.cs
│   │   │   ├── EmployeeService.cs
│   │   │   ├── PermissionService.cs
│   │   │   ├── DictService.cs
│   │   │   ├── DashboardService.cs
│   │   │   ├── DashboardConfigService.cs
│   │   │   └── MessageService.cs
│   │   ├── Common/
│   │   │   ├── Interfaces/
│   │   │   └── Dtos.cs
│   │   └── DependencyInjection.cs
│   ├── EAMS2026.Infrastructure/
│   │   ├── Data/
│   │   │   ├── Repositories/      # 12个Dapper仓储实现
│   │   │   └── DbConnectionFactory.cs
│   │   ├── Services/
│   │   │   ├── JwtService.cs
│   │   │   ├── ExcelService.cs
│   │   │   ├── PrintService.cs
│   │   │   └── DynamicDataEngine.cs
│   │   ├── Authorization/         # 自定义权限策略
│   │   │   ├── PermissionRequirement.cs
│   │   │   ├── PermissionHandler.cs
│   │   │   └── RequirePermissionAttribute.cs
│   │   ├── Migrations/            # 8个数据库迁移脚本
│   │   └── DependencyInjection.cs
│   ├── EAMS2026.Api/
│   │   ├── Controllers/           # 13个REST控制器
│   │   ├── Middleware/            # 全局异常 + 操作日志中间件
│   │   ├── Program.cs             # 应用启动配置
│   │   └── appsettings.json       # 配置文件
│   └── EAMS2026.Web/              # Vue前端
│       ├── src/
│       │   ├── api/               # API调用封装
│       │   ├── components/        # 公共组件
│       │   ├── views/             # 页面组件
│       │   ├── stores/            # Pinia状态管理
│       │   ├── router/            # Vue Router配置
│       │   ├── directives/        # 自定义指令（v-permission）
│       │   └── utils/             # 工具函数
│       ├── vite.config.ts         # Vite配置（支持局域网访问）
│       └── package.json
├── docs/
│   ├── 软件设计文档_v0.1.260526.md
│   └── 软件使用手册_v0.1.260526.md
├── tests/                         # 测试脚本和SQL
└── .trae/                         # Trae工具配置
```

### 3.3 数据库连接配置（v0.1.260526 实际配置）

#### PostgreSQL 主库（appsettings.json）
```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Port=5432;Database=eams2026;Username=postgres;Password=postgres"
  },
  "Jwt": {
    "SecretKey": "EAMS2026_SuperSecretKey_AtLeast32Characters!",
    "Issuer": "EAMS2026",
    "Audience": "EAMS2026",
    "ExpireMinutes": 1440
  }
}
```

#### SQL Server 2005（U8）—— 使用 FreeTDS（待配置）
```bash
# 安装
sudo apt-get install freetds-bin freetds-dev tdsodbc

# /etc/freetds/freetds.conf
[U8ERP]
    host = <U8服务器IP>
    port = 1433
    tds version = 7.2
    client charset = UTF-8

# 测试
tsql -S U8ERP -U <user> -P <pwd>
```

#### C# 连接字符串
```csharp
// SQL Server 2005
"Server=<IP>;Database=U8Data;User Id=<user>;Password=<pwd>;Encrypt=false;"

// SQL Server 2012（考勤机）
"Server=<IP>;Database=Attendance;User Id=<user>;Password=<pwd>;Encrypt=false;"

// PostgreSQL（当前实际配置）
"Host=localhost;Port=5432;Database=eams2026;Username=postgres;Password=postgres;"
```

### 3.4 SQL Server 2005 语法限制（关键！）
| 禁止使用的特性 | 替代方案 |
|--------------|----------|
| `OFFSET / FETCH` | 使用 `ROW_NUMBER() OVER(...)` 分页 |
| `OUTPUT` 子句 | 先查询再更新，或使用 `SCOPE_IDENTITY()` |
| `SNAPSHOT` 隔离级别 | 使用 `READ COMMITTED` |
| `TRY...CATCH` 中的某些语法 | 使用标准 `RAISERROR` |
| `CONCAT` 函数 | 使用 `+` 运算符 |

### 3.5 为Rust预留的替换接口（考勤模块）

```csharp
// EAMS2026.Domain/Interfaces/IAttendanceRepository.cs
public interface IAttendanceRepository
{
    Task<IEnumerable<AttendanceRecord>> GetRecordsAsync(DateTime start, DateTime end);
    Task<int> SyncNewRecordsAsync(DateTime lastSyncTime);
    Task<AttendanceSummary> GetTodaySummaryAsync();
}
```

初期使用C#直连SQL Server 2012实现，未来可通过配置切换到Rust代理（HTTP调用），无需修改业务代码。

### 3.6 打印方案（v0.1.260526 已实现）
- 使用 **PuppeteerSharp** 将HTML/CSS转为PDF。
- 每个审批业务类型一个HTML打印模板，严格复刻原Word样式。
- 不长期存储PDF，按需生成。

### 3.7 权限系统设计（v0.1.260526 已实现）
- **后端**：自定义 `PermissionHandler` + `IAuthorizationRequirement`，控制器标注 `[Authorize(Policy = "模块编码")]`
- **前端**：`v-permission` 指令控制元素显示，路由守卫检查Token过期
- **超级管理员**：`roles.code = 'super_admin'` 拥有所有权限
- **权限编码规则**：菜单级（如 `user`）、按钮级（如 `user:create`）

### 3.8 局域网访问支持（v0.1.260526 已实现）
- **后端**：Kestrel 绑定 `0.0.0.0:5106`，支持局域网访问
- **前端**：Vite 配置 `host: '0.0.0.0'`，支持局域网访问
- **CORS策略**：开发环境允许所有来源，生产环境从配置文件读取

---

## 第四部分：开发顺序（里程碑）

| 阶段 | 模块 | 输出 | 备份点 | 状态 |
|------|------|------|--------|------|
| **阶段1** | 基础域（部门、用户、角色、认证、日志、字典、仪表盘、消息） | 完整可运行的后端+前端 | ✅ `v0.1.260526` | ✅ 已完成 |
| **阶段2** | 考勤域（考勤机+钉钉同步、查询、异常、报表） | 考勤功能模块 | `v2.0-attendance` | ⏳ 待开发 |
| **阶段3** | 签呈审批（动态模板、流程配置、打印） | 审批模块 | `v3.0-approval` | ⏳ 待开发 |
| **阶段4** | ERP批量修改+签回 | U8集成模块 | `v4.0-erp` | ⏳ 待开发 |
| **阶段5** | 金税+报表 | 财务模块 | `v5.0-final` | ⏳ 待开发 |

---

## 第五部分：Trae CN 提示词库

### 5.1 通用提示词技巧
- **给角色**：`你是一名精通.NET 8、Dapper和SQL Server 2005兼容性的后端架构师`
- **分步走**：`第一步：生成PostgreSQL表结构；第二步：生成仓储接口...`
- **给约束**：`禁止使用EF Core，必须兼容SQL Server 2005语法`
- **结构化输入**：按 `目标 → 输入 → 输出 → 约束` 组织

### 5.2 模块专用提示词模板

#### 模板A：数据库DDL生成
```
你是一名数据库架构师，熟悉PostgreSQL。
目标：为EAMS2026系统的{模块名}生成DDL。
要求：表名小写+下划线，包含created_at/created_by/updated_at/updated_by/is_deleted。
业务需求：{描述字段和关系}
请只输出SQL语句。
```

#### 模板B：C#仓储（Dapper）
```
你是一名.NET 8 + Dapper后端工程师。
目标：实现{实体}的仓储接口和Dapper实现。
技术约束：{兼容2005|使用标准SQL}
接口定义：{粘贴}
输出：完整代码。
```

#### 模板C：U8兼容查询（SQL Server 2005）
```
你熟悉SQL Server 2005语法。
目标：为U8表{表名}生成分页查询。
约束：禁止OFFSET/FETCH，使用ROW_NUMBER()。
查询条件：{描述}
请输出SQL。
```

#### 模板D：Vue动态表单（JSON Schema）
```
你是Vue 3前端工程师。
目标：根据JSON Schema动态生成Element Plus表单。
Schema：{粘贴}
要求：支持string/enum/date类型，必填验证。
输出Vue组件代码。
```

### 5.3 阶段1启动提示词（已完成）

```
EAMS2026项目开始阶段1开发（基础模块）。

技术栈：
- 后端：.NET 8 Web API + Dapper + PostgreSQL
- 前端：Vue 3 + TypeScript + Pinia + Element Plus
- 部署：Debian 12 + Nginx

需求：实现部门管理、用户管理、角色管理、JWT认证、操作日志、数据字典、导入导出（Excel）、打印服务（HTML转PDF）。

第一步：生成PostgreSQL的数据库表结构DDL，包含以下表：
- departments (部门，树形)
- employees (员工档案)
- users (用户，关联员工，含密码哈希)
- roles (角色)
- user_roles (用户-角色)
- permissions (权限)
- role_permissions (角色-权限)
- operation_logs (操作日志)
- dict_types (字典类型)
- dict_items (字典项)

要求：表名小写+下划线，包含created_at、created_by、updated_at、updated_by、is_deleted字段。

请只输出SQL DDL语句。
```

### 5.4 阶段2考勤模块提示词（待开发）

```
EAMS2026项目阶段2：考勤管理模块。

已知：
- 考勤机数据库：SQL Server 2012，表结构如下：[粘贴]
- 钉钉考勤API：[appKey/appSecret]，接口返回格式：[粘贴]
- 现有迟到/早退规则：[描述]

需求：
1. 创建PostgreSQL表 attendance_records (存储合并后的打卡记录)
2. 实现定时同步服务（考勤机 + 钉钉），支持增量拉取
3. 提供打卡记录查询API（按人员、日期、来源筛选）
4. 实现迟到/早退自动标记逻辑
5. 提供月度统计报表API

要求：
- 考勤机访问使用Dapper，连接字符串不加Encrypt=false（2012支持）
- 钉钉API调用使用HttpClient，需处理频率限制
- 同步任务使用BackgroundService

请从生成PostgreSQL表DDL开始。
```

### 5.5 U8批量修改提示词示例（待开发）

```
你是一名熟悉U8 ERP和SQL Server 2005的.NET 8后端工程师。

目标：实现U8销售订单的批量修改功能，支持批量修改预发货日期和客户。

技术约束：
- U8数据库为SQL Server 2005，禁止OFFSET/FETCH/OUTPUT
- 使用Dapper
- 修改操作必须使用事务
- 记录操作日志到PostgreSQL

业务规则：
- 修改日期：批量修改dPreDate字段
- 修改客户：同时更新cCusCode和cCusName
- 仅允许修改未审核订单

请生成：
1. 兼容2005的分页查询SQL（含筛选条件）
2. 批量修改的事务处理代码
3. Controller的API定义
```

---

## 第六部分：Trae CN 操作流程与注意事项

### 6.1 基本操作流程
1. **打开Trae CN**，创建/打开项目文件夹。
2. **输入提示词**（可从文档第五部分复制）。
3. **按 `Ctrl+Enter`** 提交。
4. **检查输出**：
   - 符合预期 → 复制到对应文件。
   - 不符合 → 补充约束条件（如"不要使用OFFSET"），重新生成。
5. **集成测试** → 继续下一个提示词。

### 6.2 代码修改流程
1. 选中需要修改的代码块。
2. 点击悬浮菜单中的"添加到对话"。
3. 描述修改需求（例如："将这个SQL改为兼容SQL Server 2005"）。
4. 复制Trae CN输出的修改后代码，替换原代码。

### 6.3 重要注意事项

| 情况 | 应对方法 |
|------|----------|
| Trae生成了`OFFSET/FETCH` | 在每个SQL提示词中强调"**必须兼容SQL Server 2005，禁止OFFSET/FETCH**" |
| Trae忘记之前的约束 | 每个提示词都**重复关键约束**，不要假设它记住上下文 |
| 输出不完整 | 使用分步策略："第一步做X，第二步做Y，现在先做第一步" |
| 引入未声明的库（如EF Core） | 明确写："使用Dapper，不要引入Entity Framework Core" |
| 打印样式不匹配 | 提供Word样式的详细描述或截图作为参考 |
| 对话过长导致响应中断 | 分模块新建对话，每个功能模块单独开始 |

### 6.4 备份策略
- **阶段1完成后**：✅ 已完成 v0.1.260526 版本，已生成设计文档和使用手册
- **每个阶段完成后**：打标签 `v2.0-attendance`、`v3.0-approval` 等
- **重要实验前**：复制整个项目文件夹进行临时备份

---

## 第七部分：下一步行动清单

### 阶段1 已完成 ✅
- [x] .NET 8、PostgreSQL、Node.js 环境已准备
- [x] 基础模块全部实现（部门、用户、角色、认证、日志、字典、仪表盘、消息）
- [x] JWT认证 + BCrypt密码哈希
- [x] 自定义权限策略 + 前端 v-permission 指令
- [x] Excel导入导出（EPPlus）
- [x] 打印服务（PuppeteerSharp）
- [x] 局域网访问支持
- [x] 软件设计文档和使用手册已生成

### 阶段2 准备（考勤模块）
- [ ] 能连接到考勤机 SQL Server 2012
- [ ] 钉钉企业内部应用已创建，可获得 `AppKey` 和 `AppSecret`
- [ ] 提供考勤机数据库表结构（`CREATE TABLE` 语句）
- [ ] 提供现有考勤程序的迟到/早退判断规则

### 阶段3 准备（签呈审批）
- [ ] 提供现有Word审批模板样式
- [ ] 确认审批流程的节点和角色

### 阶段4 准备（ERP集成）
- [ ] 能连接到U8 SQL Server 2005（测试 `tsql -S U8ERP`）
- [ ] 确认U8销售订单和发货单的表结构

### 阶段5 准备（金税）
- [ ] 确认电子税务局API是否开放
- [ ] 如未开放，提供现有导入导出程序的文件格式

---

## 附录：v0.1.260526 版本特性总结

### 已实现的核心功能
1. **完整的RBAC权限系统**：用户、角色、权限三级管理，支持菜单级和按钮级权限控制
2. **JWT认证**：Token过期检查、自动登出、强制修改密码
3. **操作日志**：自动记录增删改操作，支持按用户、模块、日期筛选
4. **数据字典**：两级结构（类型+项），支持前端动态下拉选项
5. **仪表盘**：统计卡片 + 最近操作日志列表
6. **仪表盘配置**：动态组件管理，支持5种数据源类型（count/sql/api/recent_logs/unread_messages）
7. **站内消息**：收件箱、已发送、写消息、未读消息数
8. **Excel导入导出**：员工数据批量导入导出，支持模板下载
9. **打印服务**：HTML转PDF，支持自定义模板
10. **局域网访问**：后端和前端均支持0.0.0.0绑定

### 已修复的问题
1. JWT密钥硬编码 → 从配置文件读取
2. CORS允许所有来源 → 开发/生产环境分离
3. 列表接口无分页 → 添加分页支持
4. 登录表单预填密码 → 移除硬编码凭据
5. GetUserId()静默返回0 → 抛出UnauthorizedAccessException
6. 操作日志中间件内存问题 → 添加响应体大小限制
7. 前端图标重复注册 → 移除局部导入
8. 权限指令可靠性问题 → 改用display:none
9. 无响应压缩 → 添加Gzip压缩
10. Token过期无前端处理 → 添加Token过期检查
11. 局域网无法访问 → Kestrel和Vite配置0.0.0.0

### 生成的文档
- [软件设计文档_v0.1.260526.md](file:///d:/Codes/Projects/eams2026/docs/软件设计文档_v0.1.260526.md)：包含技术架构、数据模型、API接口清单、组件树、安全设计、部署说明
- [软件使用手册_v0.1.260526.md](file:///d:/Codes/Projects/eams2026/docs/软件使用手册_v0.1.260526.md)：包含操作流程、参数说明、字典管理示例、权限编码速查表

---

**文档结束**

您现在可以保存此文档，并按照第六部分的操作流程在Trae CN中开始阶段2（考勤模块）的开发。如有任何需要调整的地方，请随时告知。