# EAMS2026 考勤管理模块 - 软件设计文档

## 文档版本：V0.1
## 创建日期：2026-05-26
## 模块版本：v0.2-attendance
## 基于：d:\Codes\Projects\eams2026\Project_to_be_Imported\attendance

---

## 一、需求分析

### 1.1 业务背景
根据现有考勤功能代码分析，本模块需要实现以下核心业务：

| 业务需求 | 描述 | 来源文件 |
|---------|------|----------|
| 考勤数据同步 | 从考勤机（SQL Server 2012 HWATT数据库）和钉钉API同步考勤数据 | `AttendanceBLL.cs`, `Attendance2_proc.sql` |
| 打卡记录查询 | 按员工、日期范围筛选考勤记录 | `VAttendanceReport/Index.razor` |
| 考勤异常处理 | 自动识别迟到/早退/缺卡，支持手动标记事件 | `Attendance2EventDeclared.cs` |
| 考勤统计报表 | 月度出勤统计、费用计算 | `VAttendanceReport.cs` |
| 排班计划管理 | 配置班次、排班标准时间、日期类型 | `Attendance2PlanTime.cs`, `Attendance2SchemeClass.cs` |
| 假日调休管理 | 管理节假日和调休日期 | `Attendance2Holiday.cs` |
| 考勤打印 | 生成考勤报表PDF，支持打印输出 | `PrintView.razor` |
| 考勤计算 | 自动排班、时长计算、费用计算 | `Attendance2_proc.sql` |

### 1.3 外部数据库依赖

由于HWATT数据库（考勤机数据库）无法迁移到PostgreSQL，系统需要同时连接两个数据库：

| 数据库 | 类型 | 用途 | 连接方式 |
|--------|------|------|----------|
| PostgreSQL | 主数据库 | 存储业务数据、考勤记录 | 默认连接 |
| SQL Server 2012 (HWATT) | 外部数据库 | 读取员工信息和打卡记录 | 只读连接 |

**HWATT数据库表结构：**

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| `KQZ_Employee` | 员工信息表 | `EmployeeID`, `EmployeeName`, `BrchID` |
| `KQZ_Card` | 打卡记录表 | `EmployeeID`, `CardTime` |

**同步策略：**
- 员工信息：每次同步时从HWATT读取，缓存到PG的 `v_attendance_employee` 视图
- 打卡记录：考勤计算前先从HWATT同步到PG的 `attendance_records` 表

### 1.2 功能范围

#### 1.2.1 已实现功能（从现有代码继承）
| 功能模块 | 功能点 | 状态 |
|---------|--------|------|
| 基础数据管理 | 日期类型管理 | ✅ |
| | 班次管理 | ✅ |
| | 排班计划管理 | ✅ |
| | 假日调休管理 | ✅ |
| | 排班计划与班次关联 | ✅ |
| 考勤数据管理 | 考勤记录管理 | ✅ |
| | 考勤事件声明 | ✅ |
| 考勤同步 | 钉钉考勤同步 | ✅ |
| 考勤报表 | 月度考勤报表 | ✅ |
| | 考勤打印 | ✅ |
| | Excel导出 | ✅ |

#### 1.2.2 需要新增/改造功能
| 功能模块 | 功能点 | 状态 |
|---------|--------|------|
| 考勤同步 | 考勤机数据同步（SQL Server 2012） | ⏳ |
| 权限控制 | 基于RBAC的考勤管理权限 | ⏳ |
| 前端适配 | Blazor → Vue 3 页面迁移 | ⏳ |

---

## 二、架构设计

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                      前端层 (Vue 3)                              │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐│
│  │ 考勤报表页  │ │ 考勤记录页  │ │ 排班管理页  │ │ 基础数据页  ││
│  └──────┬──────┘ └──────┬──────┘ └──────┬──────┘ └──────┬──────┘│
│         │               │               │               │       │
└─────────┼───────────────┼───────────────┼───────────────┼───────┘
          │               │               │               │
          ▼               ▼               ▼               ▼
┌─────────────────────────────────────────────────────────────────┐
│                      API层 (.NET 8)                              │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │         AttendanceController                            │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐   │   │
│  │  │ Report   │ │ Record   │ │ Plan     │ │ Sync     │   │   │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘   │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      应用层                                     │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │         AttendanceService                               │   │
│  │  - 考勤数据计算（迟到/早退/加班）                        │   │
│  │  - 费用计算                                             │   │
│  │  - 报表数据聚合                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │         DingTalkService                                 │   │
│  │  - 钉钉API调用                                          │   │
│  │  - Token管理                                            │   │
│  │  - 考勤数据获取                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      领域层                                     │
│  ┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐│
│  │   考勤记录实体    │ │   排班计划实体    │ │   事件声明实体    ││
│  │                  │ │                  │ │                  ││
│  │ AttendanceRecord │ │ AttendancePlan   │ │ EventDeclared    ││
│  │ AttendanceReport │ │ SchemeClass      │ │ Holiday          ││
│  │ DayType          │ │ PlanRefClass     │ │ VEmployee        ││
│  └──────────────────┘ └──────────────────┘ └──────────────────┘│
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      基础设施层                                  │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐      │
│  │ PostgreSQL│ │ SQLServer │ │ 钉钉API   │ │   Dapper  │      │
│  │   主库    │ │ 考勤机/ERP│ │           │ │   ORM     │      │
│  └───────────┘ └───────────┘ └───────────┘ └───────────┘      │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 模块划分

| 模块 | 职责 | 位置 |
|------|------|------|
| **Attendance.Domain** | 领域实体、仓储接口、业务规则 | `src/EAMS2026.Domain/Entities/Attendance/` |
| **Attendance.Application** | 业务服务、DTO、验证逻辑 | `src/EAMS2026.Application/Services/Attendance/` |
| **Attendance.Infrastructure** | 仓储实现、外部服务集成 | `src/EAMS2026.Infrastructure/Data/Repositories/Attendance/` |
| **Attendance.Api** | REST API控制器 | `src/EAMS2026.Api/Controllers/AttendanceController.cs` |
| **Attendance.Web** | Vue前端页面组件 | `src/EAMS2026.Web/src/views/attendance/` |

---

## 三、数据模型设计

### 3.1 核心实体关系图

```
┌─────────────────┐      1:N      ┌─────────────────┐
│ AttendanceDayType│◄─────────────│AttendancePlanTime│
│  (日期类型)      │              │  (排班计划时间)  │
├─────────────────┤              ├─────────────────┤
│ ID (PK)         │              │ ID (PK)         │
│ DayTypeName     │              │ PlanName        │
│ DayTypeCaption  │              │ DayTypeId (FK)  │
└─────────────────┘              │ BTime (上班时间) │
                                 │ ETime (下班时间) │
                                 └────────┬────────┘
                                          │ 1:N
                                          ▼
┌─────────────────┐      N:N      ┌─────────────────┐      1:N      ┌─────────────────┐
│AttendanceSchemeClass│◄─────────►│AttendancePlanRefClass│◄─────────┤AttendanceRecord │
│    (班次信息)     │              │   (排班计划关联)    │              │   (考勤记录)     │
├─────────────────┤              ├─────────────────┤              ├─────────────────┤
│ ID (PK)         │              │ ID (PK)         │              │ ID (PK)         │
│ ClassName       │              │ PlanId (FK)     │              │ EmployeeId (FK) │
│ ClassDescription│              │ ClassId (FK)    │              │ SDate (日期)    │
│ ClassPeriods    │              │ PeriodNo        │              │ BAttTime        │
└─────────────────┘              └─────────────────┘              │ EAttTime        │
                                                                 │ BOffset         │
                                                                 │ EOffset         │
                                                                 │ DayTypeId (FK)  │
                                                                 │ PlanId (FK)     │
                                                                 │ ClassId (FK)    │
                                                                 └────────┬────────┘
                                                                          │ 1:N
                                                                          ▼
                                                         ┌──────────────────────────┐
                                                         │   AttendanceEventDeclared│
                                                         │      (考勤事件声明)       │
                                                         ├──────────────────────────┤
                                                         │ ID (PK)                  │
                                                         │ RecordId (FK)            │
                                                         │ EventDescription         │
                                                         │ Fee (费用)               │
                                                         │ IsBeginTime (是否上班)    │
                                                         └──────────────────────────┘
```

### 3.2 实体详细设计

#### 3.2.1 AttendanceRecord（考勤记录）

| 字段名 | 类型 | 约束 | 说明 | 来源 |
|--------|------|------|------|------|
| ID | int | PK, Identity | 记录ID | `Attendance2Record.cs:20` |
| EmployeeId | int | FK | 员工ID | `Attendance2Record.cs:26` |
| PeriodNo | int | - | 周期内次序号 | `Attendance2Record.cs:33` |
| SDate | DateTime | - | 考勤日期 | `Attendance2Record.cs:40` |
| BAttTime | TimeSpan? | - | 上班考勤时间 | `Attendance2Record.cs:49` |
| EAttTime | TimeSpan? | - | 下班考勤时间 | `Attendance2Record.cs:55` |
| BOffset | int? | - | 上班偏移量（分钟） | `Attendance2Record.cs:61` |
| EOffset | int? | - | 下班偏移量（分钟） | `Attendance2Record.cs:67` |
| BOffsetFee | decimal? | - | 上班考勤费用 | `Attendance2Record.cs:70` |
| EOffsetFee | decimal? | - | 下班考勤费用 | `Attendance2Record.cs:73` |
| BTime | TimeSpan? | - | 上班标准时间 | `Attendance2Record.cs:79` |
| ETime | TimeSpan? | - | 下班标准时间 | `Attendance2Record.cs:85` |
| DayTypeId | int? | FK | 日期类型ID | `Attendance2Record.cs:92` |
| PlanId | int? | FK | 排班计划ID | `Attendance2Record.cs:100` |
| ClassId | int? | FK | 班次ID | `Attendance2Record.cs:109` |

#### 3.2.2 AttendanceReport（考勤报表视图）

| 字段名 | 类型 | 约束 | 说明 | 来源 |
|--------|------|------|------|------|
| ID | int | PK | 记录ID | `Attendance2VReport.cs:19` |
| EmployeeId | int | - | 员工ID | `Attendance2VReport.cs:22` |
| EmployeeName | string | - | 员工姓名 | `Attendance2VReport.cs:24` |
| SDate | DateTime | - | 日期 | `Attendance2VReport.cs:26` |
| Description | string | - | 周描述 | `Attendance2VReport.cs:28` |
| BAttTime | TimeSpan? | - | 上班时间 | `Attendance2VReport.cs:30` |
| BOffset | int? | - | 上班时长（分钟） | `Attendance2VReport.cs:32` |
| EAttTime | TimeSpan? | - | 下班时间 | `Attendance2VReport.cs:34` |
| EOffset | int? | - | 下班时长（分钟） | `Attendance2VReport.cs:36` |
| Event | string | - | 事由 | `Attendance2VReport.cs:38` |
| bOffsetFee | decimal? | - | 上班事件费用 | `Attendance2VReport.cs:40` |
| eOffsetFee | decimal? | - | 下班事件费用 | `Attendance2VReport.cs:42` |
| Fee | decimal? | - | 总费用 | `Attendance2VReport.cs:45` |

#### 3.2.3 AttendanceSchemeClass（班次信息）

| 字段名 | 类型 | 约束 | 说明 | 来源 |
|--------|------|------|------|------|
| ID | int | PK, Identity | 班次ID | `Attendance2SchemeClass.cs:21` |
| ClassName | string | - | 班次名称 | `Attendance2SchemeClass.cs:27` |
| Periods | int? | - | 总排班数 | `Attendance2SchemeClass.cs:34` |
| ClassDescription | string | - | 班次描述 | `Attendance2SchemeClass.cs:40` |
| ClassPeriods | int? | - | 班次内周期 | `Attendance2SchemeClass.cs:46` |

#### 3.2.4 AttendancePlanTime（排班计划时间）

| 字段名 | 类型 | 约束 | 说明 | 来源 |
|--------|------|------|------|------|
| ID | int | PK, Identity | 计划ID | `Attendance2PlanTime.cs:23` |
| PlanName | string | - | 计划名称 | `Attendance2PlanTime.cs:29` |
| DayTypeId | int | FK | 日期类型ID | `Attendance2PlanTime.cs:36` |
| Description | string | - | 描述 | `Attendance2PlanTime.cs:44` |
| BTime | TimeSpan | - | 开始时间 | `Attendance2PlanTime.cs:50` |
| ETime | TimeSpan | - | 结束时间 | `Attendance2PlanTime.cs:57` |

#### 3.2.5 AttendanceDayType（日期类型）

| 字段名 | 类型 | 约束 | 说明 | 来源 |
|--------|------|------|------|------|
| ID | int | PK, Identity | 日期类型ID | `Attendance2DayType.cs:21` |
| DayTypeName | string | - | 日期类型名称 | `Attendance2DayType.cs:27` |
| DayTypeCaption | string | - | 日期类型标题 | `Attendance2DayType.cs:33` |

#### 3.2.6 AttendanceEventDeclared（考勤事件声明）

| 字段名 | 类型 | 约束 | 说明 | 来源 |
|--------|------|------|------|------|
| ID | int | PK, Identity | 事件ID | `Attendance2EventDeclared.cs:18` |
| EventDescription | string | - | 事件描述 | `Attendance2EventDeclared.cs:24` |
| Fee | decimal? | - | 费用 | `Attendance2EventDeclared.cs:30` |
| Memo | string | - | 备注 | `Attendance2EventDeclared.cs:36` |
| CheckMan | string | - | 审核人 | `Attendance2EventDeclared.cs:44` |
| Manager | string | - | 经理签字 | `Attendance2EventDeclared.cs:48` |
| RecordId | int | FK | 考勤记录ID | `Attendance2EventDeclared.cs:52` |
| IsBeginTime | bool | - | 是否上班考勤事件 | `Attendance2EventDeclared.cs:56` |

#### 3.2.7 AttendanceHoliday（假日调休）

| 字段名 | 类型 | 约束 | 说明 | 来源 |
|--------|------|------|------|------|
| ID | int | PK, Identity | 序号 | `Attendance2Holiday.cs:22` |
| IYear | int | Required | 年份 | `Attendance2Holiday.cs:29` |
| SDate | DateTime? | Required | 日期 | `Attendance2Holiday.cs:38` |
| SName | string | - | 节假日名称 | `Attendance2Holiday.cs:44` |
| BTime | TimeSpan? | - | 上班时间 | `Attendance2Holiday.cs:51` |
| ETime | TimeSpan? | - | 下班时间 | `Attendance2Holiday.cs:58` |
| SDescription | string | - | 节假日描述 | `Attendance2Holiday.cs:64` |

#### 3.2.8 VAttendanceEmployee（员工视图）

| 字段名 | 类型 | 约束 | 说明 | 来源 |
|--------|------|------|------|------|
| ID | int | PK | 员工ID | `Attendance2VEmployee.cs:14` |
| EmployeeName | string | - | 员工姓名 | `Attendance2VEmployee.cs:17` |

### 3.3 数据库表结构

#### 3.3.1 attendance_records（考勤记录表）

```sql
CREATE TABLE attendance_records (
    id SERIAL PRIMARY KEY,
    employee_id INTEGER NOT NULL,
    period_no INTEGER NOT NULL,
    s_date DATE NOT NULL,
    b_att_time TIME,
    e_att_time TIME,
    b_offset INTEGER,
    e_offset INTEGER,
    b_offset_fee DECIMAL(10, 2),
    e_offset_fee DECIMAL(10, 2),
    b_time TIME,
    e_time TIME,
    day_type_id INTEGER,
    plan_id INTEGER,
    class_id INTEGER,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    created_by VARCHAR(50),
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_by VARCHAR(50),
    is_deleted BOOLEAN DEFAULT FALSE,
    FOREIGN KEY (employee_id) REFERENCES v_attendance_employee(id),
    FOREIGN KEY (day_type_id) REFERENCES attendance_day_types(id),
    FOREIGN KEY (plan_id) REFERENCES attendance_plan_times(id),
    FOREIGN KEY (class_id) REFERENCES attendance_scheme_classes(id)
);
```

#### 3.3.2 attendance_plan_times（排班计划表）

```sql
CREATE TABLE attendance_plan_times (
    id SERIAL PRIMARY KEY,
    plan_name VARCHAR(100) NOT NULL,
    day_type_id INTEGER NOT NULL,
    description VARCHAR(500),
    b_time TIME NOT NULL,
    e_time TIME NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    created_by VARCHAR(50),
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_by VARCHAR(50),
    is_deleted BOOLEAN DEFAULT FALSE,
    FOREIGN KEY (day_type_id) REFERENCES attendance_day_types(id)
);
```

#### 3.3.3 attendance_scheme_classes（班次表）

```sql
CREATE TABLE attendance_scheme_classes (
    id SERIAL PRIMARY KEY,
    class_name VARCHAR(100) NOT NULL,
    periods INTEGER,
    class_description VARCHAR(500),
    class_periods INTEGER,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    created_by VARCHAR(50),
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_by VARCHAR(50),
    is_deleted BOOLEAN DEFAULT FALSE
);
```

#### 3.3.4 attendance_plan_ref_classes（排班计划关联表）

```sql
CREATE TABLE attendance_plan_ref_classes (
    id SERIAL PRIMARY KEY,
    class_id INTEGER,
    plan_id INTEGER NOT NULL,
    period_no INTEGER,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    created_by VARCHAR(50),
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_by VARCHAR(50),
    is_deleted BOOLEAN DEFAULT FALSE,
    FOREIGN KEY (class_id) REFERENCES attendance_scheme_classes(id),
    FOREIGN KEY (plan_id) REFERENCES attendance_plan_times(id)
);
```

#### 3.3.5 attendance_day_types（日期类型表）

```sql
CREATE TABLE attendance_day_types (
    id SERIAL PRIMARY KEY,
    day_type_name VARCHAR(50) NOT NULL,
    day_type_caption VARCHAR(50),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    created_by VARCHAR(50),
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_by VARCHAR(50),
    is_deleted BOOLEAN DEFAULT FALSE
);
```

#### 3.3.6 attendance_event_declareds（考勤事件声明表）

```sql
CREATE TABLE attendance_event_declareds (
    id SERIAL PRIMARY KEY,
    event_description VARCHAR(500),
    fee DECIMAL(10, 2),
    memo VARCHAR(500),
    check_man VARCHAR(50),
    manager VARCHAR(50),
    record_id INTEGER NOT NULL,
    is_begin_time BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    created_by VARCHAR(50),
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_by VARCHAR(50),
    is_deleted BOOLEAN DEFAULT FALSE,
    FOREIGN KEY (record_id) REFERENCES attendance_records(id)
);
```

#### 3.3.7 attendance_holidays（假日调休表）

```sql
CREATE TABLE attendance_holidays (
    id SERIAL PRIMARY KEY,
    i_year INTEGER NOT NULL,
    s_date DATE NOT NULL,
    s_name VARCHAR(100),
    b_time TIME,
    e_time TIME,
    s_description VARCHAR(500),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    created_by VARCHAR(50),
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_by VARCHAR(50),
    is_deleted BOOLEAN DEFAULT FALSE
);
```

#### 3.3.8 attendance_fee_calculators（费用计算器表）

```sql
CREATE TABLE attendance_fee_calculators (
    id SERIAL PRIMARY KEY,
    day_type_id INTEGER NOT NULL,
    range_a INTEGER NOT NULL,
    range_b INTEGER NOT NULL,
    range_price DECIMAL(10, 2) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    created_by VARCHAR(50),
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_by VARCHAR(50),
    is_deleted BOOLEAN DEFAULT FALSE,
    FOREIGN KEY (day_type_id) REFERENCES attendance_day_types(id)
);
```

#### 3.3.9 attendance_employee_ref_scheme_classes（员工班次关联表）

```sql
CREATE TABLE attendance_employee_ref_scheme_classes (
    id SERIAL PRIMARY KEY,
    employee_id INTEGER NOT NULL,
    class_id INTEGER NOT NULL,
    period_no INTEGER,
    eff_date DATE NOT NULL,
    exp_date DATE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    created_by VARCHAR(50),
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_by VARCHAR(50),
    is_deleted BOOLEAN DEFAULT FALSE,
    FOREIGN KEY (class_id) REFERENCES attendance_scheme_classes(id)
);
```

---

## 三、考勤计算算法设计（从SQL存储过程迁移）

### 3.4 日期类型定义

| DayTypeID | 类型名称 | 说明 |
|-----------|----------|------|
| 1 | WorkDay | 工作日 |
| 2 | Duty | 值班 |
| 3 | Over | 加班 |
| 4 | DayOff | 假日 |
| 5 | Holiday | 节日 |
| 6 | Absence | 缺勤 |
| 7 | Leave | 请假 |

### 3.5 核心计算流程

#### 3.5.1 考勤记录保存（Attendance_SaveAttendanceRecord）

**输入参数：**
- `empId`: 员工ID
- `sDate`: 考勤日期
- `cardBegin`: 上班打卡时间
- `cardEnd`: 下班打卡时间

**处理逻辑：**
1. 查询员工当日是否已有考勤记录
2. 如果不存在，插入新记录
3. 如果存在，更新记录：
   - 上班时间取较早的时间（打卡时间 vs 已有记录）
   - 下班时间取较晚的时间（打卡时间 vs 已有记录）

#### 3.5.2 单日排班计算（Attendance_scheduleOneDay）

**输入参数：**
- `eid`: 员工ID
- `scheduleDate`: 排班日期

**处理逻辑：**
1. **获取上日考勤信息**：查询上一日的周期序号(`periodNo`)和排班计划ID(`planid`)
2. **计算周期序号**：`period2 = (prev_period2 + 1) % periods`，若为0则取`periods`
3. **获取班次标准时间**：根据周期序号查询当日的上班时间(`bTime`)、下班时间(`eTime`)
4. **检查生效日**：如果当日是新排班计划的生效日，重新获取排班信息
5. **检查节日**：查询假日表，若为节日则更新日期类型和时间
6. **周期复位**：如果是特殊计划(planid=11,12)且为周六，执行周期复位
7. **保存排班记录**：插入或更新考勤记录

#### 3.5.3 周期复位算法（Attendance_resetSchedulePeriod）

**触发条件：**
- 当前计划为节日计划(planid=11,12)且日期为周六

**处理逻辑：**
1. 获取班次周期数(`periods`)和班次内周期数(`classPeriods`)
2. 计算复位后的周期序号：`period = (@periods + (@period - @classPeriods)) % @periods`

#### 3.5.4 单日考勤计算（Attendance_reCalculaorOneDay）

**输入参数：**
- `eid`: 员工ID
- `attDate`: 考勤日期

**处理逻辑：**
1. **查询考勤记录**：获取当日的打卡时间、标准时间、日期类型等信息
2. **计算时长偏移**：
   - `bOverTime = DATEDIFF(mi, bAttTime, bTime)` - 上班偏移（负数表示迟到）
   - `eOverTime = DATEDIFF(mi, eTime, eAttTime)` - 下班偏移（正数表示加班）
   - 特殊计划(planid=7,8,12)：`bOverTime=0`, `eOverTime = DATEDIFF(mi, bAttTime, eAttTime)`
3. **费用计算**：根据`dayTypeId`和偏移量从`attendance_fee_calculators`表查询费用
4. **更新记录**：更新`bOffset`, `eOffset`, `bOffsetFee`, `eOffsetFee`字段

#### 3.5.5 从HWATT导入数据（Attendance_ImportAttendanceDeviceHWATT）

**输入参数：**
- `empId`: 员工ID
- `beginDate`: 开始日期
- `endDate`: 结束日期

**处理逻辑：**
1. 遍历日期范围
2. 从HWATT数据库查询当日打卡记录：
   ```sql
   SELECT CAST(CardTime AS date) as dd, min(CardTime) as b, max(CardTime) as e 
   FROM HWATT.dbo.KQZ_Card
   WHERE EmployeeID=@empId AND CAST(CardTime AS date)=@beginDate
   GROUP BY CAST(CardTime AS date)
   ```
3. 调用`Attendance_SaveAttendanceRecord`保存记录

#### 3.5.6 全员工考勤计算（Attendance_doAllEmployeeAttendance）

**输入参数：**
- `doDate`: 开始日期
- `endDate`: 结束日期（默认当月最后一天）

**处理逻辑：**
1. 遍历所有有效员工（从`Attendance2_EmployeeRefSchemeClass`查询）
2. 对每个员工，遍历日期范围
3. 依次执行：
   - `Attendance_scheduleOneDay` - 自动排班
   - `Attendance_reCalculaorOneDay` - 计算时长和费用

---

## 四、API接口设计

### 4.1 接口总览

| API路径 | HTTP方法 | 所属控制器 | 功能描述 |
|---------|----------|-----------|----------|
| `/api/attendance/report/search` | POST | AttendanceController | 查询考勤报表 |
| `/api/attendance/report/export` | POST | AttendanceController | 导出Excel |
| `/api/attendance/report/import-from-device` | POST | AttendanceController | 从考勤机导入 |
| `/api/attendance/report/import-from-dingtalk` | POST | AttendanceController | 从钉钉导入 |
| `/api/attendance/report/schedule` | POST | AttendanceController | 排班计算 |
| `/api/attendance/report/recalc` | POST | AttendanceController | 重新计算考勤 |
| `/api/attendance/records` | GET | AttendanceController | 获取考勤记录列表 |
| `/api/attendance/records/{id}` | GET | AttendanceController | 获取考勤记录详情 |
| `/api/attendance/records` | POST | AttendanceController | 创建考勤记录 |
| `/api/attendance/records/{id}` | PUT | AttendanceController | 更新考勤记录 |
| `/api/attendance/records/{id}` | DELETE | AttendanceController | 删除考勤记录 |
| `/api/attendance/plan-times` | GET | AttendanceController | 获取排班计划列表 |
| `/api/attendance/plan-times/{id}` | GET | AttendanceController | 获取排班计划详情 |
| `/api/attendance/plan-times` | POST | AttendanceController | 创建排班计划 |
| `/api/attendance/plan-times/{id}` | PUT | AttendanceController | 更新排班计划 |
| `/api/attendance/plan-times/{id}` | DELETE | AttendanceController | 删除排班计划 |
| `/api/attendance/scheme-classes` | GET | AttendanceController | 获取班次列表 |
| `/api/attendance/scheme-classes/{id}` | GET | AttendanceController | 获取班次详情 |
| `/api/attendance/scheme-classes` | POST | AttendanceController | 创建班次 |
| `/api/attendance/scheme-classes/{id}` | PUT | AttendanceController | 更新班次 |
| `/api/attendance/scheme-classes/{id}` | DELETE | AttendanceController | 删除班次 |
| `/api/attendance/day-types` | GET | AttendanceController | 获取日期类型列表 |
| `/api/attendance/day-types/{id}` | GET | AttendanceController | 获取日期类型详情 |
| `/api/attendance/day-types` | POST | AttendanceController | 创建日期类型 |
| `/api/attendance/day-types/{id}` | PUT | AttendanceController | 更新日期类型 |
| `/api/attendance/day-types/{id}` | DELETE | AttendanceController | 删除日期类型 |
| `/api/attendance/events` | GET | AttendanceController | 获取事件列表 |
| `/api/attendance/events/{id}` | GET | AttendanceController | 获取事件详情 |
| `/api/attendance/events` | POST | AttendanceController | 创建事件 |
| `/api/attendance/events/{id}` | PUT | AttendanceController | 更新事件 |
| `/api/attendance/events/{id}` | DELETE | AttendanceController | 删除事件 |
| `/api/attendance/holidays` | GET | AttendanceController | 获取假日列表 |
| `/api/attendance/holidays/{id}` | GET | AttendanceController | 获取假日详情 |
| `/api/attendance/holidays` | POST | AttendanceController | 创建假日 |
| `/api/attendance/holidays/{id}` | PUT | AttendanceController | 更新假日 |
| `/api/attendance/holidays/{id}` | DELETE | AttendanceController | 删除假日 |
| `/api/attendance/employees` | GET | AttendanceController | 获取考勤员工列表 |
| `/api/attendance/employees/{id}` | GET | AttendanceController | 获取考勤员工详情 |

### 4.2 接口详细设计

#### 4.2.1 考勤报表查询

**POST** `/api/attendance/report/search`

请求体：
```json
{
  "employeeId": 1,
  "startDate": "2026-05-01",
  "endDate": "2026-05-31"
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| employeeId | int | 否 | 员工ID，为空则查询所有员工 |
| startDate | string | 是 | 开始日期 (yyyy-MM-dd) |
| endDate | string | 是 | 结束日期 (yyyy-MM-dd) |

响应体：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "items": [
      {
        "id": 1,
        "employeeId": 1,
        "employeeName": "张三",
        "sDate": "2026-05-15",
        "description": "周三",
        "bAttTime": "08:30:00",
        "bOffset": -15,
        "eAttTime": "18:15:00",
        "eOffset": 15,
        "event": "正常出勤",
        "fee": 15.00
      }
    ],
    "total": 31,
    "page": 1,
    "pageSize": 32
  }
}
```

#### 4.2.2 从考勤机导入数据

**POST** `/api/attendance/report/import-from-device`

请求体：
```json
{
  "employeeId": 1,
  "startDate": "2026-05-01",
  "endDate": "2026-05-31"
}
```

响应体：
```json
{
  "code": 200,
  "message": "success",
  "data": 15 // 导入记录数
}
```

#### 4.2.3 从钉钉导入数据

**POST** `/api/attendance/report/import-from-dingtalk`

请求体：
```json
{
  "employeeId": 1,
  "startDate": "2026-05-01",
  "endDate": "2026-05-31"
}
```

响应体：
```json
{
  "code": 200,
  "message": "success",
  "data": 15 // 导入记录数
}
```

#### 4.2.4 排班计算

**POST** `/api/attendance/report/schedule`

请求体：
```json
{
  "employeeId": 1,
  "startDate": "2026-05-01",
  "endDate": "2026-05-31"
}
```

响应体：
```json
{
  "code": 200,
  "message": "success",
  "data": 31 // 计算记录数
}
```

#### 4.2.5 重新计算考勤

**POST** `/api/attendance/report/recalc`

请求体：
```json
{
  "employeeId": 1,
  "startDate": "2026-05-01",
  "endDate": "2026-05-31"
}
```

响应体：
```json
{
  "code": 200,
  "message": "success",
  "data": 31 // 重新计算记录数
}
```

#### 4.2.6 获取考勤记录列表

**GET** `/api/attendance/records?page=1&pageSize=20&employeeId=1&startDate=2026-05-01&endDate=2026-05-31`

响应体：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "items": [
      {
        "id": 1,
        "employeeId": 1,
        "periodNo": 1,
        "sDate": "2026-05-15",
        "bAttTime": "08:30:00",
        "eAttTime": "18:15:00",
        "bOffset": -15,
        "eOffset": 15,
        "bOffsetFee": null,
        "eOffsetFee": 15.00,
        "bTime": "08:45:00",
        "eTime": "18:00:00",
        "dayTypeId": 1,
        "planId": 1,
        "classId": 1,
        "createdAt": "2026-05-15T08:30:00",
        "createdBy": "admin"
      }
    ],
    "total": 100,
    "page": 1,
    "pageSize": 20
  }
}
```

#### 4.2.7 创建考勤事件

**POST** `/api/attendance/events`

请求体：
```json
{
  "recordId": 1,
  "eventDescription": "迟到",
  "fee": -30.00,
  "memo": "交通拥堵",
  "checkMan": "李四",
  "manager": "王五",
  "isBeginTime": true
}
```

响应体：
```json
{
  "code": 200,
  "message": "创建成功",
  "data": {
    "id": 1,
    "recordId": 1,
    "eventDescription": "迟到",
    "fee": -30.00,
    "memo": "交通拥堵",
    "checkMan": "李四",
    "manager": "王五",
    "isBeginTime": true,
    "createdAt": "2026-05-15T10:00:00"
  }
}
```

---

## 五、业务流程设计

### 5.1 考勤数据同步流程

```
┌──────────────┐    ┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│  考勤机(SQL  │    │   钉钉API    │    │   排班计算   │    │  考勤重算    │
│   Server2012)│    │              │    │              │    │              │
└──────┬───────┘    └──────┬───────┘    └──────┬───────┘    └──────┬───────┘
       │                   │                   │                   │
       ▼                   ▼                   ▼                   ▼
┌───────────────────────────────────────────────────────────────────────┐
│                    AttendanceService                                  │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │  1. 从考勤机获取打卡记录                                        │  │
│  │  2. 从钉钉API获取打卡记录                                        │  │
│  │  3. 合并去重（同一员工同一天取最早/最晚时间）                       │  │
│  │  4. 根据排班计划计算标准时间                                      │  │
│  │  5. 计算迟到/早退/加班偏移量                                      │  │
│  │  6. 根据费用规则计算费用                                         │  │
│  │  7. 保存到attendance_records表                                   │  │
│  └─────────────────────────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
                    ┌────────────────┐
                    │ PostgreSQL主库 │
                    └────────────────┘
```

### 5.2 考勤报表查询流程

```
前端请求 ──────► AttendanceController ──────► AttendanceService
     │                    │                          │
     │                    │                          ▼
     │                    │              查询attendance_records
     │                    │              关联查询员工信息
     │                    │              聚合计算费用
     │                    ▼                          │
     │              返回PagedResult                  │
     │                    │                          │
     │                    │◄─────────────────────────│
     ▼                    ▼
前端展示 ◄─────── JSON响应
```

### 5.3 钉钉API调用流程

```
请求考勤数据 ──────► DingTalkService ──────► 获取Token
     │                    │                      │
     │                    │                      ▼
     │                    │              检查Token过期
     │                    │              过期则重新获取
     │                    │                      │
     │                    ▼                      │
     │              调用钉钉考勤API ◄─────────────┘
     │                    │
     │                    ▼
     │              解析响应数据
     │                    │
     │                    ▼
     │              转换为AttendanceRecord
     │                    │
     ▼                    ▼
返回考勤记录 ◄─────── 数据转换完成
```

---

## 六、前端页面设计

### 6.1 页面结构

| 页面路径 | 页面名称 | 功能描述 | 来源参考 |
|----------|----------|----------|----------|
| `/attendance/report` | 考勤报表 | 查看月度考勤报表、导入数据、打印 | `VAttendanceReport/Index.razor` |
| `/attendance/report/record/{id}` | 考勤记录详情 | 查看单条考勤记录及关联事件 | `VAttendanceReport/Record.razor` |
| `/attendance/records` | 考勤记录管理 | CRUD操作 | `Attendance2Record/Index.razor` |
| `/attendance/plan-times` | 排班计划管理 | CRUD操作 | `Attendance2PlanTime/Index.razor` |
| `/attendance/scheme-classes` | 班次管理 | CRUD操作 | `Attendance2SchemeClass/Index.razor` |
| `/attendance/day-types` | 日期类型管理 | CRUD操作 | `Attendance2DayType/Index.razor` |
| `/attendance/holidays` | 假日管理 | CRUD操作 | `Attendance2Holiday/Index.razor` |
| `/attendance/events` | 事件管理 | CRUD操作 | `Attendance2EventDeclared/Index.razor` |

### 6.2 考勤报表页面设计（重点保持原布局）

根据 `VAttendanceReport/Index.razor`，考勤报表页面需要保持以下布局：

#### 6.2.1 搜索区域
- 员工选择下拉框
- 日期范围选择器

#### 6.2.2 报表表格（原样实现）
| 列名 | 宽度 | 样式 | 功能 |
|------|------|------|------|
| 日期(dd) | 20px | 居中 | 显示日期日份 |
| 周 | 74px | 左对齐 | 显示星期描述 |
| 上班时间 | 60px | 居中 | 显示HH:mm格式 |
| 上班时长 | 35px | 按钮样式 | 负数红色(迟到)，正数绿色(加班)，点击可查看/编辑事件 |
| 下班时间 | 60px | 居中 | 显示HH:mm格式 |
| 下班时长 | 35px | 按钮样式 | 负数红色(早退)，正数绿色(加班)，点击可查看/编辑事件 |
| 事由 | 240px | 自动换行 | 显示事件描述 |
| 费用 | 35px | 右对齐 | 显示费用金额 |

#### 6.2.3 工具栏按钮
| 按钮 | 权限要求 | 功能 |
|------|----------|------|
| 导入考勤(全员) | 考勤管理员 | 从考勤机+钉钉导入所有员工数据 |
| 导入考勤(个人) | 导入权限 | 从考勤机+钉钉导入当前员工数据 |
| 打印 | 所有用户 | 打开打印预览对话框 |
| 导出Excel | 导出权限 | 导出当前查询结果 |

#### 6.2.4 进度条
- 导入操作时显示进度条
- 进度分为四个阶段：考勤机导入(25%) → 钉钉导入(50%) → 排班计算(75%) → 考勤重算(100%)

### 6.3 打印视图页面设计

根据 `VAttendanceReport/PrintView.razor`，打印视图需要保持以下布局：

#### 6.3.1 页面尺寸
- 宽度：649px
- 高度：964px（适配A4打印）

#### 6.3.2 页面结构
```
┌─────────────────────────────────────────────────────────┐
│ Header: 考勤 [员工姓名] [考勤年月]                      │
├─────────────────────────────────────────────────────────┤
│ Table: 日期 | 周 | 上班时间 | 上班时长 | 上班审批      │
│        | 下班时间 | 下班时长 | 下班审批 | 事由 | 费用   │
├─────────────────────────────────────────────────────────┤
│ Footer: 缺勤: X元  加班: Y元  主管审核:_________       │
│                  总经理审批:_________                  │
└─────────────────────────────────────────────────────────┘
```

#### 6.3.3 样式要求
- 字体：14px/1.25 "Helvetica Neue", Helvetica, Arial
- 表格边框：1px 实线黑色
- 不显示按钮和操作列

### 6.4 页面组件结构

```
views/attendance/
├── report/
│   ├── index.vue          # 考勤报表主页面
│   ├── record.vue         # 考勤记录详情弹窗
│   └── print-view.vue     # 打印视图组件
├── records/
│   ├── index.vue          # 考勤记录列表
│   ├── form.vue           # 考勤记录表单(新增/编辑)
│   └── detail.vue         # 考勤记录详情
├── plan-times/
│   ├── index.vue          # 排班计划列表
│   └── form.vue           # 排班计划表单
├── scheme-classes/
│   ├── index.vue          # 班次列表
│   └── form.vue           # 班次表单
├── day-types/
│   ├── index.vue          # 日期类型列表
│   └── form.vue           # 日期类型表单
├── holidays/
│   ├── index.vue          # 假日列表
│   └── form.vue           # 假日表单
└── events/
    ├── index.vue          # 事件列表
    └── form.vue           # 事件表单
```

---

## 七、钉钉集成设计

### 7.1 钉钉API配置

| 配置项 | 值 | 说明 | 来源 |
|--------|------|------|------|
| AppKey | dingolaz4gqcrbdponui | 钉钉应用Key | `DingTalkBase.cs:36` |
| AppSecret | --gIaMyXIQM23OFOxmItZUvhgDIy-f_VGtAFxa7O0KDwNaPT2NrfdGHb-R2O_TVE | 钉钉应用密钥 | `DingTalkBase.cs:37` |
| Token有效期 | 7200秒(2小时) | AccessToken过期时间 | `DingTalkBase.cs:22` |
| API域名 | https://oapi.dingtalk.com | 钉钉开放平台域名 | `DingTalkAttendance.cs:69` |

### 7.2 钉钉API调用封装

#### 7.2.1 Token管理

```csharp
// 参考 DingTalkBase.cs
public class DingTalkTokenService
{
    private static string _token = string.Empty;
    private static DateTime _tokenTime = DateTime.MinValue;
    
    public async Task<string> GetTokenAsync()
    {
        if (DateTime.Now >= _tokenTime.AddSeconds(7200))
        {
            await RefreshTokenAsync();
        }
        return _token;
    }
    
    private async Task RefreshTokenAsync()
    {
        // 调用钉钉gettoken接口获取新Token
        // 更新_token和_tokenTime
    }
}
```

#### 7.2.2 考勤数据获取

```csharp
// 参考 DingTalkAttendance.cs
public class DingTalkAttendanceService
{
    // 获取考勤组信息
    public async Task<AtteGroupInfo> GetAttendanceGroupAsync()
    
    // 获取部门列表
    public async Task<List<Department>> GetDepartmentsAsync()
    
    // 获取部门用户列表
    public async Task<List<User>> GetUsersAsync(long departmentId)
    
    // 获取考勤记录
    public async Task<List<DingTalkAttendanceRecord>> GetAttendancesAsync(
        DateRange dateRange, List<string> userIdList)
    
    // 获取单个用户考勤
    public async Task<List<DingTalkAttendanceRecord>> GetUserAttendanceAsync(
        string empName, DateRange dateRange)
}
```

### 7.3 数据转换逻辑

```
钉钉考勤记录 ──────► DingTalkAttendanceRecord ──────► AttendanceRecord
     │                       │                           │
     │  userCheckDateTime    │  SDate (日期部分)         │
     │  workDate             │  BAttTime (最早打卡时间)   │
     │  BaseCheckTime        │  EAttTime (最晚打卡时间)   │
     │                       │                           │
     └───────────────────────┴───────────────────────────┘
```

---

## 八、权限设计

### 8.1 权限编码规则

| 权限编码 | 权限名称 | 说明 |
|----------|----------|------|
| attendance | 考勤管理 | 考勤模块根权限 |
| attendance:report | 考勤报表 | 查看考勤报表 |
| attendance:import | 导入考勤 | 导入考勤数据 |
| attendance:export | 导出考勤 | 导出考勤报表 |
| attendance:print | 打印考勤 | 打印考勤报表 |
| attendance:manage | 考勤管理 | 考勤管理员权限 |
| attendance:records | 考勤记录 | 管理考勤记录 |
| attendance:plan | 排班计划 | 管理排班计划 |
| attendance:class | 班次管理 | 管理班次 |
| attendance:daytype | 日期类型 | 管理日期类型 |
| attendance:event | 事件管理 | 管理考勤事件 |
| attendance:holiday | 假日管理 | 管理假日调休 |

### 8.2 角色权限映射

| 角色 | 权限 |
|------|------|
| 超级管理员(super_admin) | 所有权限 |
| 考勤管理员 | attendance, attendance:report, attendance:import, attendance:export, attendance:print, attendance:manage, attendance:records, attendance:plan, attendance:class, attendance:daytype, attendance:event, attendance:holiday |
| 部门负责人 | attendance, attendance:report, attendance:export, attendance:print |
| 普通员工 | attendance, attendance:report |

---

## 九、部署与集成

### 9.1 依赖与环境

| 依赖 | 版本 | 说明 |
|------|------|------|
| .NET 8 | 8.0+ | 后端运行环境 |
| PostgreSQL | 18+ | 主数据库 |
| SQL Server | 2012+ | 考勤机数据库(只读) |
| Node.js | 20+ | 前端构建 |
| Vue 3 | 3.4+ | 前端框架 |
| Element Plus | 2.6+ | UI组件库 |

### 9.2 配置说明

#### 9.2.1 appsettings.json 新增配置

```json
{
  "DingTalk": {
    "AppKey": "dingolaz4gqcrbdponui",
    "AppSecret": "--gIaMyXIQM23OFOxmItZUvhgDIy-f_VGtAFxa7O0KDwNaPT2NrfdGHb-R2O_TVE",
    "ApiBaseUrl": "https://oapi.dingtalk.com"
  },
  "ConnectionStrings": {
    "HWATT": "Server=<HWATT数据库IP>;Database=HWATT;User Id=<user>;Password=<pwd>;Encrypt=false;"
  }
}
```

#### 9.2.2 数据库视图定义（PostgreSQL）

**v_attendance_employee 视图（员工信息视图）**

```sql
CREATE VIEW v_attendance_employee AS
SELECT 
    employee_id AS id,
    employee_name
FROM attendance_employees
WHERE is_deleted = FALSE;
```

**v_attendance_report 视图（考勤报表视图）**

```sql
CREATE VIEW v_attendance_report AS
SELECT 
    r.id AS record_id,
    r.employee_id,
    e.employee_name,
    r.s_date,
    p.description,
    r.b_att_time,
    r.b_offset,
    r.e_att_time,
    r.e_offset,
    r.b_offset_fee,
    r.e_offset_fee,
    (SELECT string_agg(
        CASE WHEN ed.is_begin_time THEN '早:' ELSE '晚:' END || ed.event_description, 
        ';' ORDER BY ed.is_begin_time
    ) FROM attendance_event_declareds ed WHERE ed.record_id = r.id) AS event,
    (SELECT MAX(ed.fee) FROM attendance_event_declareds ed WHERE ed.record_id = r.id AND ed.fee > 0) AS fee
FROM attendance_records r
LEFT JOIN attendance_plan_times p ON r.plan_id = p.id
LEFT JOIN v_attendance_employee e ON r.employee_id = e.id
WHERE r.is_deleted = FALSE;
```

**说明**：原SQL Server视图中的`V_AttendanceEmployee`从`HWATT.dbo.KQZ_Employee`读取数据，由于HWATT无法迁移，改为从本地`attendance_employees`表读取，需要在同步时先将员工信息从HWATT同步到本地。

### 9.3 数据库迁移

需要创建以下迁移脚本：

| 迁移文件 | 内容 |
|----------|------|
| 001_CreateAttendanceTables.cs | 创建考勤相关表结构 |
| 002_AddAttendanceIndexes.cs | 添加索引 |
| 003_InitialAttendanceData.cs | 初始化基础数据（日期类型、班次等） |

---

## 十、代码安全性

### 10.1 注意事项

| 风险点 | 风险等级 | 关联模块 | 说明 |
|--------|----------|----------|------|
| 钉钉密钥硬编码 | 高 | DingTalkBase | 原代码中AppKey/AppSecret硬编码在代码中 |
| SQL注入 | 中 | 考勤机连接 | 需要使用Dapper参数化查询 |
| 敏感数据泄露 | 中 | 考勤记录 | 员工考勤数据属于敏感信息 |
| 未授权访问 | 中 | 所有API | 需要RBAC权限控制 |
| 日志注入 | 低 | 操作日志 | 操作日志记录需要过滤特殊字符 |

### 10.2 解决方案

| 风险点 | 解决方案 |
|--------|----------|
| 钉钉密钥硬编码 | 将密钥移至配置文件，使用环境变量存储敏感配置 |
| SQL注入 | 使用Dapper参数化查询，禁止拼接SQL |
| 敏感数据泄露 | API返回数据脱敏处理，日志中不记录敏感信息 |
| 未授权访问 | 所有接口添加权限验证，使用JWT Token |
| 日志注入 | 对日志内容进行转义处理，限制日志长度 |

---

## 附录：现有代码文件清单

| 文件路径 | 文件类型 | 说明 |
|----------|----------|------|
| `em_wtm.Model/_Business/Attendance/Attendance2Record.cs` | C#类 | 考勤记录实体 |
| `em_wtm.Model/_Business/Attendance/Attendance2VReport.cs` | C#类 | 考勤报表视图实体 |
| `em_wtm.Model/_Business/Attendance/Attendance2SchemeClass.cs` | C#类 | 班次信息实体 |
| `em_wtm.Model/_Business/Attendance/Attendance2PlanTime.cs` | C#类 | 排班计划实体 |
| `em_wtm.Model/_Business/Attendance/Attendance2DayType.cs` | C#类 | 日期类型实体 |
| `em_wtm.Model/_Business/Attendance/Attendance2EventDeclared.cs` | C#类 | 考勤事件实体 |
| `em_wtm.Model/_Business/Attendance/Attendance2Holiday.cs` | C#类 | 假日调休实体 |
| `em_wtm.Model/_Business/Attendance/Attendance2PlanRefClass.cs` | C#类 | 排班计划关联实体 |
| `em_wtm.Model/_Business/Attendance/Attendance2VEmployee.cs` | C#类 | 员工视图实体 |
| `em_wtm.BLL/AttendanceBLL.cs` | C#类 | 考勤业务逻辑 |
| `em_wtm.BLL/DingTalkDFL/DingTalkAttendance.cs` | C#类 | 钉钉考勤集成 |
| `em_wtm.BLL/DingTalkDFL/DingTalkBase.cs` | C#类 | 钉钉基础服务 |
| `em_wtm.Shared/Pages/Attendance/VAttendanceReport/Index.razor` | Blazor页面 | 考勤报表主页面 |
| `em_wtm.Shared/Pages/Attendance/VAttendanceReport/Record.razor` | Blazor页面 | 考勤记录详情 |
| `em_wtm.Shared/Pages/Attendance/VAttendanceReport/PrintView.razor` | Blazor页面 | 打印视图 |

---

**文档结束**

本设计文档基于现有考勤功能代码分析生成，包含完整的实体设计、API设计、业务流程和前端页面设计。前端考勤报表页面将保持与原Blazor版本相同的布局和样式，以减少用户学习成本。